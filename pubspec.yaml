name: timetable
description: A clean Flutter course timetable app.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  add_2_calendar: ^2.1.2
  dynamic_color: ^1.8.1
  file_picker: ^8.1.7
  flutter:
    sdk: flutter
  html: ^0.15.4
  path_provider: ^2.1.5
  shared_preferences: ^2.5.3
  webview_flutter: ^4.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
